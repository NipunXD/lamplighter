# secrets — never commit
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
build/
.turbo/
.cache/
*.log
.DS_Store

# local data
data/runtime/
*.sqlite
*.sqlite-journal

# rendered film + narration (large; upload the mp4, do not commit it)
film/out/
